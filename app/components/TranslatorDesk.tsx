"use client";
import {useEffect,useState} from "react";
import {supabase} from "../supabaseClient";
import {ROHILLA_LANGUAGES} from "../lib/rohilla-languages";
import RealtimeInterpreter from "./RealtimeInterpreter";

declare global {interface Window{webkitSpeechRecognition?:any;SpeechRecognition?:any}}

async function translate(text:string,target:string,source:string){const r=await fetch("/api/translate",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({text,target,source})});const j=await r.json();return {text:j?.translations?.[0]||text,configured:Boolean(j?.configured),error:j?.error}}

export default function TranslatorDesk({title="Language Operations",defaultOperator="hi-IN",actorRole}:{title?:string;defaultOperator?:string;actorRole?:"admin"|"dealer"|"partner"}){
 const db=supabase();const [access,setAccess]=useState(actorRole?"checking":"ready");const [customerLang,setCustomerLang]=useState("kn-IN");const [operatorLang,setOperatorLang]=useState(defaultOperator);const [customerText,setCustomerText]=useState("");const [operatorView,setOperatorView]=useState("");const [operatorReply,setOperatorReply]=useState("");const [customerReply,setCustomerReply]=useState("");const [note,setNote]=useState("");const [busy,setBusy]=useState(false);
 useEffect(()=>{if(!actorRole)return;(async()=>{const {data:{session}}=await db.auth.getSession();if(!session){setAccess("denied");setNote("Sign-in is required.");return}if(actorRole==="admin"){const [{data:aal},{data:isAdmin}]=await Promise.all([db.auth.mfa.getAuthenticatorAssuranceLevel(),db.rpc("is_admin")]);if(aal?.currentLevel!=="aal2"||!isAdmin){setAccess("denied");setNote("Administrator authentication is required.");return}setAccess("ready");return}const {data:p}=await db.from("profiles").select("role,active").eq("id",session.user.id).single();if(!p||p.role!==actorRole||!p.active){setAccess("denied");setNote(`Approved ${actorRole==="partner"?"partner":"dealer"} access is required.`);return}setAccess("ready")})()},[actorRole]);
 function speech(which:"customer"|"operator"){const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR){setNote("Voice input is not available in this browser.");return}const r=new SR();r.lang=which==="customer"?customerLang:operatorLang;r.interimResults=false;r.onresult=(e:any)=>{const v=e.results?.[0]?.[0]?.transcript||"";if(which==="customer")setCustomerText(v);else setOperatorReply(v)};r.onerror=()=>setNote("Voice input stopped. Type the message instead.");r.start()}
 async function toOperator(){if(!customerText.trim())return;setBusy(true);setNote("Translating customer message…");const x=await translate(customerText,operatorLang,customerLang);setOperatorView(x.text);setNote(x.configured?"Customer message translated successfully.":"Translation service is not connected. The original text has been preserved.");setBusy(false)}
 async function toCustomer(){if(!operatorReply.trim())return;setBusy(true);setNote("Translating reply…");const x=await translate(operatorReply,customerLang,operatorLang);setCustomerReply(x.text);setNote(x.configured?"Customer reply translated successfully.":"Translation service is not connected. The original reply has been preserved.");setBusy(false)}
 async function copy(text:string){if(!text)return;await navigator.clipboard.writeText(text);setNote("Copied to clipboard.")}
 if(access!=="ready")return <main><section className="section"><h1>{title}</h1><p>{access==="checking"?"Checking secure access…":note}</p></section></main>;
 return <main><header><div className="brand"><b>ROHILLA DRIVE</b><small>{title}</small></div></header><section className="hero"><div className="heroText"><span>MULTILINGUAL CUSTOMER COMMUNICATION</span><h1>Communicate Across Languages</h1><p>Translate customer messages into your working language and prepare replies in the customer's selected language while preserving the original message for reference.</p></div></section><section className="section"><div className="grid"><article className="application"><h2>Customer to Team</h2><label>Customer language</label><select value={customerLang} onChange={e=>setCustomerLang(e.target.value)}>{ROHILLA_LANGUAGES.map(l=><option key={l.code} value={l.code}>{l.nativeName} — {l.name}</option>)}</select><textarea style={{width:"100%",minHeight:130,marginTop:10}} value={customerText} onChange={e=>setCustomerText(e.target.value)} placeholder="Customer message or voice transcript"/><div className="row"><button onClick={()=>speech("customer")}>Customer Voice Input</button><button onClick={toOperator} disabled={busy||!customerText.trim()}>Translate for Team</button></div><textarea style={{width:"100%",minHeight:130,marginTop:10}} value={operatorView} readOnly placeholder="Translated team view"/><button onClick={()=>copy(operatorView)} disabled={!operatorView}>Copy Translation</button></article><article className="application"><h2>Team to Customer</h2><label>Working language</label><select value={operatorLang} onChange={e=>setOperatorLang(e.target.value)}>{ROHILLA_LANGUAGES.map(l=><option key={l.code} value={l.code}>{l.nativeName} — {l.name}</option>)}</select><textarea style={{width:"100%",minHeight:130,marginTop:10}} value={operatorReply} onChange={e=>setOperatorReply(e.target.value)} placeholder="Team reply"/><div className="row"><button onClick={()=>speech("operator")}>Team Voice Input</button><button onClick={toCustomer} disabled={busy||!operatorReply.trim()}>Translate for Customer</button></div><textarea style={{width:"100%",minHeight:130,marginTop:10}} value={customerReply} readOnly placeholder="Reply in the customer's language"/><button onClick={()=>copy(customerReply)} disabled={!customerReply}>Copy Customer Reply</button></article></div>{note&&<div className="notice">{note}</div>}</section><section className="section"><h2>Live Speech Interpretation</h2><p>Use one direction at a time so the system can apply the correct source and target languages.</p><div className="grid"><RealtimeInterpreter targetLanguage={operatorLang} label="Customer speaks → team hears working language"/><RealtimeInterpreter targetLanguage={customerLang} label="Team speaks → customer hears selected language"/></div></section><section className="section dark"><div className="about"><h2>Live Interpretation Availability</h2><p>Live browser interpretation is available only when an authorised real-time language provider is connected. Telephone-call interpretation requires a separately authorised telephony integration.</p></div></section></main>
}
